[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run build && NODE_ENV=production npm start"
healthcheckPath = "/api"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[[services]]
name = "clinicms-backend" 